export interface User {
  userID: string;
  name: string;
  email: string;
  groupNumber: string;
}

export interface Student {
  userID: string;
  name: string;
  subGroup: string;
  email: string;
  batch: string;
}
