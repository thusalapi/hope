export interface User {
  id: string;
  name: string;
  email: string;
  groupNumber: string;
}

export interface Student {
  batch: string;
  subGroup: string;
  email: string;
  groupNumber: string;
  name: string;
}
